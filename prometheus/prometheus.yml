global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'invoice-backend'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['backend:8089']
        labels:
          application: 'Invoice Reader'
